# Auto detect text files and perform LF normalization
* text=auto

# Binary assets. Line ending normalisation would corrupt these, and a diff of
# them is never useful. The README already promised the .glb rule; it now exists.
*.glb  binary
*.wasm binary
*.png  binary
*.wav  binary
